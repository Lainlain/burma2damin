import {
  defer,
  propsFactory
} from "./chunk-J4UJOH5G.js";

// node_modules/vuetify/lib/composables/delay.js
var makeDelayProps = propsFactory({
  closeDelay: [Number, String],
  openDelay: [Number, String]
}, "delay");
function useDelay(props, cb) {
  let clearDelay = () => {
  };
  function runDelay(isOpening, options) {
    clearDelay == null ? void 0 : clearDelay();
    const delay = Math.max((options == null ? void 0 : options.minDelay) ?? 0, Number(isOpening ? props.openDelay : props.closeDelay));
    return new Promise((resolve) => {
      clearDelay = defer(delay, () => {
        cb == null ? void 0 : cb(isOpening);
        resolve(isOpening);
      });
    });
  }
  function runOpenDelay() {
    return runDelay(true);
  }
  function runCloseDelay(options) {
    return runDelay(false, options);
  }
  return {
    clearDelay,
    runOpenDelay,
    runCloseDelay
  };
}

export {
  makeDelayProps,
  useDelay
};
//# sourceMappingURL=chunk-2KSNGK3K.js.map
