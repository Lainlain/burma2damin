# Production environment configuration
# Direct API calls to production backend (same server, port 4545)
VITE_API_BASE_URL=http://localhost:4545

# AppConfig Server (standalone server on port 8080)
# Using production domain for AppConfig server
VITE_CONFIG_SERVER_URL=https://config.shwemyanmar2d.us/api/burma2d

# Main Server URL for image uploads (same as API base)
VITE_MAIN_SERVER_URL=http://localhost:4545
